<?php
if (!empty($this->entries)) {
    /** @var $entry Posting_Row */
    foreach ($this->entries as $entry) {
        echo $this->partial('posting/_entry.phtml', array('entry' => $entry, 'route' => $entry->getRouteName(), 'isPreview' => true));
    }
} else {
    ?>
<div class="message">Записей нет</div>
<?php
}
?>