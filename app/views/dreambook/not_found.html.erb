<% content_for :meta_title, t('.title') %>

<article>
  <h1><%= t('.heading') %></h1>

  <div class="warn"><%= t('.message') %></div>

  <%= render partial: 'form', locals: { query: '' } %>

  <section>
    <%= render 'letters' %>
  </section>
</article>
