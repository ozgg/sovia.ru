<% content_for :title, t('.title') %>

<header>
  <h1><%= t('.header') %></h1>
</header>

<%= render 'greeting' %>

<%= render 'letters' %>

<%= render 'layouts/social' %>

<section>
  <%= render 'layouts/statistics' %>
  <%= render 'layouts/more' %>
</section>
