<% content_for :meta_title, t('.title') %>

<article>
  <h1><%= t('.title') %></h1>

  <%= render partial: 'form', locals: { query: @query } %>
  <% unless @query.blank? %>
      <%= render partial: 'dreambook/patterns', locals: { collection: @collection } %>
  <% end %>
</article>
