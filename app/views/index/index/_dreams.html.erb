<section class="index-dreams">
  <h2>Недавние сны</h2>

  <%=
    render(
        partial: 'dreams/list',
        locals: {
            collection: Dream.list_for_visitors(current_user).first(6),
            handler: Biovision::Components::DreamsComponent[current_user]
        }
    )
  %>

  <footer>
    <nav><%= link_to('Все сны', dreams_path, class: 'button-nav') %></nav>
  </footer>
</section>
