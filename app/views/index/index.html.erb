<% if @articles.any? %>
    <%= @articles.each do |article| %>
        <article>
          <header><%= article.title %></header>
          <div><%= simple_format(article.body) %></div>
          <footer></footer>
        </article>
    <% end %>
<% else %>
    <div class="message"><%= t('.no_articles') %></div>
<% end %>