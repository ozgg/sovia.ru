<header>
  <h1><%= t('.main') %></h1>
  <%= render 'management_links' %>
  <h1><%= t('.recent_posts') %></h1>
</header>

<%= render partial: 'posts/list', object: @posts %>

<footer>
  <%= render 'layouts/social' %>
</footer>
