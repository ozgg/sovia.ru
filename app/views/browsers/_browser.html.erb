<article class="browser">
  <h1><%= browser.name %></h1>

  <%= render partial: 'admin/flags', locals: { flags: browser.flags } %>

  <section>
    <h1><%= t('agents.index.header') %></h1>
    <%= render partial: 'admin/list_of_entities', locals: { collection: browser.agents.by_name } %>
  </section>
</article>
