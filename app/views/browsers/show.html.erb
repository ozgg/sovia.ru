<% content_for :meta_title, t('.title', name: @entity.name) %>
<% content_for :sidebar do %>
    <div class="options">
      <ul>
        <li><%= link_to t(:list), browsers_path %></li>
        <li><%= link_to t(:edit), edit_browser_path(@entity) %></li>
        <li><%= link_to t(:create), new_browser_path %></li>
      </ul>
    </div>
<% end %>

<%= render @entity %>
