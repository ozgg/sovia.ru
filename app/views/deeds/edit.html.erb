<% content_for :meta_title, t('.title') %>
<%= render 'shared/metrics' %>

<section>
  <header>
    <h1><%= t('.title') %></h1>
    <nav class="options">
      <ul>
        <li><%= link_to t(:view), @deed %></li>
        <li><%= link_to t(:list), my_deeds_path %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</section>
