<% content_for :meta_title, @entity.essence %>
<% content_for :sidebar do %>
    <div class="options">
      <ul>
        <li><%= link_to t(:edit), edit_deed_path(@entity) %></li>
        <li><%= link_to_delete @entity %></li>
      </ul>
    </div>
    <%= render 'sidebar/more_links' %>
<% end %>
<%= render 'shared/metrics' %>

<%= render @entity %>
