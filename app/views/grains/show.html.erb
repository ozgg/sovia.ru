<% content_for :meta_title, @entity.name %>
<% content_for :sidebar do %>
    <div class="options">
      <ul>
        <li><%= link_to t(:edit), edit_grain_path(@entity) %></li>
      </ul>
    </div>
<% end %>
<%= render 'shared/metrics' %>

<%= render @entity %>
