<% content_for :title, t('.title', user: params[:login], page: params[:page] || 1) %>
<% content_for :description, t('.description', user: params[:login], page: params[:page] || 1) %>

<header>
  <h1><%= t('.header', user: params[:login]) %></h1>
  <h2><%= t('page', page: params[:page] || 1) %></h2>
</header>

<%= render 'entries/list' %>

<%= render 'common/metrics' %>
