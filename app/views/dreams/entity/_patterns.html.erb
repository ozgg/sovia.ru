<section>
  <h2><%= t('.automatic_interpretation') %></h2>

  <dl class="interpretation">
    <% dream.patterns.good_for_dreambook.ordered_by_popularity.first(20).each do |pattern| %>
        <dt><%= pattern_in_dreambook pattern %></dt>
        <dd>
          <% if pattern.essence.blank? %>
              <%= t('.see_dreambook') %>
          <% else %>
              <%= pattern.essence %>
          <% end %>
        </dd>
    <% end %>
  </dl>
</section>
