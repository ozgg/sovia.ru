<% content_for :meta_title, t('.title', page: current_page) %>

<article class="dreams">
  <h1><%= t('.heading') %></h1>

  <%= paginate @collection %>
  <%= render partial: 'dreams/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
