<% content_for :meta_title, t('.title', user: @user.profile_name, page: current_page) %>
<%= render 'shared/metrics' %>

<article class="comments">
  <h1><%= t('.header', user: @user.profile_name) %></h1>

  <%= paginate @comments %>
  <%= render partial: 'comments/administrative_list', locals: { comments: @comments } %>
  <%= paginate @comments %>
</article>
