<% content_for :meta_title, t('.title', user: @user.login, page: params[:page] || 1) %>
<% content_for :meta_description, t('.description', user: @user.login, page: params[:page] || 1) %>
<%= render 'shared/metrics' %>

<section>
  <h1><%= t('.header', user: @user.login) %></h1>

  <h2><%= render 'shared/page' %></h2>

  <%= render partial: 'posts/list', locals: { posts: @posts, paginate: true } %>
</section>
