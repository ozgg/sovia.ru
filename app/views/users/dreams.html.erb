<% content_for :meta_title, t('.title', user: @user.login, page: current_page) %>
<% content_for :meta_description, t('.description', user: @user.login, page: current_page) %>
<%= render 'shared/metrics' %>

<section>
  <h1><%= t('.header', user: @user.login) %></h1>

  <h2><%= render 'shared/page' %></h2>

  <%= render 'dreams/list' %>

</section>
