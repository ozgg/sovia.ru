<ul class="external-login">
  <% %w(vkontakte twitter facebook).each do |provider| %>
      <li><%= link_to image_tag("icons/login/#{provider}.png"), auth_external_path(provider: provider), title: t(".#{provider}") %></li>
  <% end %>
</ul>
