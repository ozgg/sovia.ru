<% if current_user && current_user.moderator? %>
    <% if item.ip || item.agent %>
        <div class="track">
          <div><%= item.ip %></div>
          <div><%= item.agent.name if item.agent %></div>
        </div>
    <% end %>
<% end %>