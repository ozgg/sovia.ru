<header>
  <h1><%= @title %></h1>
  <nav>
    <ul>
      <li><%= link_to t('.new_tag'), new_entry_tag_path %></li>
    </ul>
  </nav>
</header>

<% if @tags.any? %>
    <ul>
      <% @tags.each do |tag| %>
      <li><%= link_to tag.name, tag %></li>
      <% end %>
    </ul>

    <%= paginate @tags %>
<% else %>
    <div class="message"><%= t('tags.list.no_tags') %></div>
<% end %>
