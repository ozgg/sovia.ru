<% content_for :title, t('.title') %>
<% content_for :description, t('.description') %>

<article>
  <header>
    <h1><%= t('.header') %></h1>
    <nav>
      <ul>
        <li><%= link_to t(:list), agents_path %></li>
        <li><%= link_to t(:view), @agent %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>