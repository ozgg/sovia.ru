<% content_for :meta_title, t('.title') %>

<article>
  <header>
    <h1><%= t('.title') %></h1>
    <nav class="options">
      <ul>
        <li><%= link_to t(:list), agents_path %></li>
        <li><%= link_to t(:view), @agent %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>