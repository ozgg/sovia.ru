<% content_for :meta_title, t('.title', id: @entity.id) %>
<% content_for :sidebar do %>
    <div class="options">
      <ul>
        <li><%= link_to t(:list), agents_path %></li>
        <li><%= link_to t(:edit), edit_agent_path(@entity) %></li>
        <li><%= link_to t(:create), new_agent_path %></li>
      </ul>
    </div>
<% end %>

<%= render @entity %>
