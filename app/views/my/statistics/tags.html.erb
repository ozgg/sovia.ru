<header>
  <h1><%= @title %></h1>
</header>

<% if @tags.any? %>
    <nav class="dream-symbols">
      <ul>
        <% @tags.each do |personal_tag| %>
            <li><%= personal_tag.tag.name %> (<%= personal_tag.entries_count %>)</li>
        <% end %>
      </ul>
    </nav>

    <%= paginate @tags %>
<% else %>
    <div class="message"><%= t('nothing_found') %></div>
<% end %>