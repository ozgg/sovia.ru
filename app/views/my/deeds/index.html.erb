<header>
  <h1><%= @title %></h1>
  <nav>
    <ul>
      <li><%= link_to t('.about'), deeds_path %></li>
    </ul>
  </nav>
</header>

<div>
  <h2><%= t('.create') %></h2>
  <%= render 'deeds/form' %>
</div>

<% if @deeds.any? %>
    <ul class="deeds">
      <% @deeds.each do |deed| %>
      <li>
        <time datetime="<%= deed.created_at %>"><%= deed.created_at.strftime('%d.%m.%Y') %></time>
        <%= deed.name %>
        <%= link_to '*', deed_path(deed) %>
      </li>
      <% end %>
    </ul>

    <%= paginate @deeds %>
<% else %>
    <div class="message"><%= t('.empty') %></div>
<% end %>