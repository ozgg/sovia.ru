<% content_for :title, t('.title') %>
<% content_for :description, t('.description') %>

<article>
  <header>
    <h1><%= t('.header') %></h1>
    <nav>
      <ul>
        <li><%= link_to t('view'), @goal %></li>
        <li><%= link_to t('.goals'), my_goals_path %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>