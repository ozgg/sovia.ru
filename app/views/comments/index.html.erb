<% content_for :meta_title, t('.title', page: current_page) %>

<article class="comments">
  <h1><%= t('.header') %></h1>

  <%= paginate @collection %>
  <%= render partial: 'administrative_list', locals: { comments: @collection } %>
  <%= paginate @collection %>
</article>
