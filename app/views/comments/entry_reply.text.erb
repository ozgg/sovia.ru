Здравствуйте, <%= @comment.commentable.user.name_for_letter %>.

На вашу запись «<%= @comment.commentable.title_for_view %>» на сайте sovia.ru был оставлен комментарий.
Посмотреть его можно по этому адресу:
<%= url_for @comment.commentable %>

Сам комментарий (<%= @comment.user.nil? ? 'анонимный' : @comment.user.screen_name %>):
<%= strip_tags(prepare_comment_text(@comment, @comment.commentable.user)) %>

--
  С уважением.
  автоматический уведомитель
