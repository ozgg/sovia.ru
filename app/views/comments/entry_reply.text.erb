Здравствуйте, <%= @comment.commentable.user.screen_name %>.

На вашу запись «<%= @comment.commentable.title %>» на сайте sovia.ru был оставлен комментарий.
Посмотреть его можно по этому адресу:
<%# comment_url @comment %>

Сам комментарий (<%= @comment.user.nil? ? 'анонимный' : @comment.user.screen_name %>):
<%= @comment.body %>

--
  С уважением.
  автоматический уведомитель
