<% if comments.any? %>
    <% comments.each do |comment| %>
        <% if comment.visible_to? current_user %>
            <%= render partial: 'comments/preview', locals: { comment: comment } %>
        <% else %>
            <div class="warning"><%= t(:insufficient_rights) %></div>
        <% end %>
    <% end %>
<% else %>
    <%= render 'shared/nothing_found' %>
<% end %>
