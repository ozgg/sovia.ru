<% content_for :meta_title, t('.title') %>
<%= render 'shared/metrics' %>

<article>
  <header>
    <h1><%= t('.title') %></h1>
    <nav>
      <ul>
        <li><%= link_to t(:list), posts_path %></li>
        <li><%= link_to t(:view), @post %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>
