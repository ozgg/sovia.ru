<% content_for :meta_title, t('.title', tag: @tag.name, page: current_page) %>

<article class="posts">
  <h1><%= t('.heading', tag: @tag.name) %></h1>

  <%= paginate @collection %>
  <%= render partial: 'list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>