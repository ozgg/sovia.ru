<% content_for :meta_title, t('.title', id: @account.id) %>

<nav class="options">
  <ul>
    <li><%= link_to t(:list), accounts_path %></li>
    <li><%= link_to t(:edit), edit_account_path(@account) %></li>
    <li><%= link_to t(:create), new_account_path %></li>
  </ul>
</nav>

<article>
  <h1><%= t('.title', id: @account.id) %></h1>

  <%= render @account %>
</article>