<% content_for :meta_title, t('.title') %>

<nav class="options">
  <ul>
    <li><%= link_to t(:list), accounts_path %></li>
    <li><%= link_to t(:view), @account %></li>
    <li><%= link_to t(:create), new_account_path %></li>
    <li><%= link_to t(:delete), @account, method: :delete, data: { confirm: t(:are_you_sure) } %></li>
  </ul>
</nav>

<section>
  <h1><%= t('.title') %></h1>

  <%= render 'form' %>
</section>
