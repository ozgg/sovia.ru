<header>
  <h1><%= @title %></h1>
  <% if @current_user && @current_user.editor? %>
      <nav>
        <ul>
          <li><%= link_to t('.new_article'), new_article_path %></li>
        </ul>
      </nav>
  <% end %>
</header>

<%= render partial: 'posts/list', object: @articles %>
