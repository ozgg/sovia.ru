<% content_for :meta_title, t('.title') %>
<% content_for :breadcrumbs do %>
    <span><%= t('admin.patterns.nav_item.text') %></span>
<% end %>

<article>
  <h1><%= t('.heading') %></h1>
  <ul class="actions">
    <li><%= back_icon(admin_path) %></li>
    <li><%= create_icon(new_pattern_path) %></li>
  </ul>

  <%= paginate @collection %>
  <%= render partial: 'shared/admin/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
