<% content_for :meta_title, t('.title', user: @entity.profile_name, page: current_page) %>

<article>
  <h1><%= t('.heading', user: @entity.profile_name) %></h1>
  <ul class="actions">
    <li><%= back_icon(user_profile_path(slug: @entity.long_slug)) %></li>
  </ul>

  <%= paginate @collection %>
  <%= render partial: 'profiles/comments/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
