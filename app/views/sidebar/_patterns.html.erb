<div>
  <h5><%= t('.heading') %></h5>

  <dl>
    <% Pattern.recent.first(5).each do |pattern| %>
    <div>
      <dt><%= pattern_link(pattern) %></dt>
      <dd><%= pattern.summary %></dd>
    </div>
    <% end %>
  </dl>
</div>
