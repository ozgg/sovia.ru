<% content_for :meta_title, t('.title', body: @entity.body) %>
<% content_for :sidebar do %>
    <div class="options">
      <ul>
        <li><%= link_to t(:list), codes_path %></li>
        <li><%= link_to t(:edit), edit_code_path(@entity) %></li>
        <li><%= link_to t(:create), new_code_path %></li>
      </ul>
    </div>
<% end %>

<%= render @entity %>
